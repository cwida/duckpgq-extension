set(DUCKPGQ_PEG_UPSTREAM_DIR ${CMAKE_CURRENT_SOURCE_DIR}/upstream)

file(GLOB_RECURSE DUCKPGQ_PEG_SOURCES
     CONFIGURE_DEPENDS
     ${DUCKPGQ_PEG_UPSTREAM_DIR}/src/parser/peg/*.cpp)

set(EXTENSION_SOURCES
    ${DUCKPGQ_PEG_SOURCES}
    ${EXTENSION_SOURCES}
    PARENT_SCOPE)
